<li class="media chat">
  <%# link_to image_tag(comment.user.avatar_url, alt: comment.user.name, class: "media-object"),
              comment.user.profile_url, target: '_blank', class: 'pull-left' %>
  <div class="media-body">
    <h4 class="media-heading">name<%# link_to comment.user.name, comment.user.profile_url, target: '_blank' %> says
      <small class="text-muted">[at (put time here)<%# chat.created_at.strftime('%-d %B %Y, %H:%M:%S') %>]</small></h4>
    <p><%= home.body %></p>
  </div>
</li>
